# Copy this file to .env and fill in your actual values

# Server Configuration
NODE_ENV=development
PORT=5000

# Database Configuration
MONGO_URI=mongodb://localhost:27017/mygf-ai-app

# JWT Configuration
# Generate a secure random secret with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_jwt_secret_here_use_a_long_random_string_generated_with_crypto
JWT_EXPIRE=30d

# Cloudinary Configuration
CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# CORS Configuration
FRONTEND_URL=http://localhost:3000

# Email Configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_app_password
EMAIL_FROM=MyGF AI <noreply@yourdomain.com>
EMAIL_SECURE=false

# Google OAuth Configuration
# To get your Google Client ID:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Enable Google+ API (or Google Identity Services)
# 4. Go to Credentials > Create Credentials > OAuth 2.0 Client ID
# 5. Application type: Web application
# 6. Add authorized JavaScript origins: http://localhost:3001 (and your production domain)
# 7. Add authorized redirect URIs: http://localhost:3001 (and your production domain)
# 8. Copy the Client ID and paste below
GOOGLE_CLIENT_ID=your_google_client_id_here

# Gemini AI Configuration (for AI chat features)
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL_NAME=gemini-2.5-flash

# Twilio Configuration (for WhatsApp notifications - optional)
# Get credentials from: https://console.twilio.com
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_WHATSAPP_NUMBER=whatsapp:+1234567890

# M-Pesa Configuration
# Get credentials from: https://developer.safaricom.co.ke/MyApps
# 1. Create an app on Safaricom Daraja portal
# 2. Select "Lipa Na M-Pesa Online" product
# 3. Get Consumer Key and Consumer Secret from the app
# 4. For sandbox, use test credentials from: https://developer.safaricom.co.ke/test_credentials
MPESA_ENVIRONMENT=sandbox
MPESA_CONSUMER_KEY=your_mpesa_consumer_key
MPESA_CONSUMER_SECRET=your_mpesa_consumer_secret
MPESA_BUSINESS_SHORTCODE=174379
MPESA_PASSKEY=your_mpesa_passkey
MPESA_CALLBACK_URL=https://yourdomain.com/api/payments/mpesa-callback